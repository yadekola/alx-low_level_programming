#include <stdio.h>

/**
  * main - prints the text "Programming is like a multilingual puzzle
  * Return: 0 if executed properly, if not return nonzero
  */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
