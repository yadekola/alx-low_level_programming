#include "main.h"

/**
* _islower - This code checks whether a text is lowercase or not
* @c: the character that represent lowercase here
* Return: 1 if c is lowercase, o if uppercase
*/

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
